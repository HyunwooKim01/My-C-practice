#include <iostream>

using namespace std;

int main()
{
    short a, b = 0;
    
    cin >> a >> b;
    
    if (a < b)
        cout << "<" << endl;
    else if (a > b)
        cout << ">" << endl;
    else if (a == b)
        cout << "==" << endl;
    
    

    return 0;
}
